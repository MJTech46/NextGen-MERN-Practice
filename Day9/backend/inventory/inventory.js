// Function to add a product to the products array
export function addProduct(products, product) {
    products.push(product);
    return products;
}

// Function to list all products in the products array
export function listProduct(products) {
    return products;
}